{
  "_comment": "Policy for MeetingBar. Edited values are picked up on the next detection (no recompile). Transcription language is auto-detected; 'whisper_model' pins the Whisper model. 'meeting_command' defaults to running the CLI out of this checkout — only set it to override that.",

  "meeting_bundle_ids": [
    "com.microsoft.teams2",
    "com.microsoft.teams",
    "us.zoom.xos",
    "com.tinyspeck.slackmacgap",
    "com.cisco.webexmeetingsapp",
    "com.webex.meetingmanager"
  ],

  "record_only": false,
  "whisper_model": "large-v3-turbo",

  "cooldown_seconds": 180,
  "require_meeting_app": true,

  "ignore_device_substrings": ["blackhole", "multi-output"]
}
